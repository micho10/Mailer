# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

POST        /send/email                     controllers.MailController.sendEmail
POST        /send/scala/customMailer        controllers.MailController.sendWithCustomMailer
